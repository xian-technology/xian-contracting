[package]
name = "xian-native-tracer"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Rust-backed instruction tracer for Xian"

[lib]
name = "_native"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.28.3", features = ["abi3-py314", "extension-module"] }
